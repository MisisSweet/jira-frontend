import React, { Component } from "react";

export default class ButtonDelete extends Component{

    render(){
        return(
            <button className="btn brn-danger">Удалить</button>
        )
    }
}